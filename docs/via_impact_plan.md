# Via-fix impact across cmos5l + g2 (plan)

Source: read-only discovery workflow (6 agents), Aug 2026. Full raw in the task output.

## Core conclusion
The via fix VALIDATES the density coefficients; it does not change them. Both PDKs'
cap_cmomi.va, the PCell C-label, the xschem tcleval and the qucs-s equation all already use the
with-via notes densities {N<=2:0.55, N=3:0.82, N=4:1.09, N>=5:1.36 (g2)}. Our old sparse-via
layout sat ~14% below them; the fix raises the drawn layout onto them (N=4: 0.884->1.011 ES).
=> no coefficient edit, no electrical golden regen, no osdi rebuild FROM the via fix.

## In-flight state (entangled, pre-existing)
- cmos5l `fix/cap-cmomi-row-count` (10 commits, unmerged): row-count floor(w/0.89) [drops the -1],
  feed refit CFEED_SLOPE=0.1625/CFEED_END=0.0916, regenerated goldens+osdi, new tests. No vias.
- g2 `fix/cap-cmomi-row-and-feed` (4 commits, unmerged): same, plus qucs-s artifacts. No vias.
- Via fix: cmos5l PCell has it but UNCOMMITTED on main (same file the branch edits); g2 PCell
  still base-only.

## What the via fix requires (NO new simulation)
1. PCell: commit cmos5l tip-via fix; apply identical TOOTH_VIA_OFF=0.41 fix to g2 cmomi_code.py;
   update PCell docstrings ("via at every tooth/bar overlap" -> base+tip). g2 needs its own DRC
   confirm (M5/Via4 tiers).
2. Geometry-dependent artifacts (via geometry changed): re-pin cmos5l geometry_test FREEZE
   digests (7 sha256 over all layers incl vias); regenerate LVS testcase GDS (both repos); re-run
   cap_cmomi_sweep DRC/LVS (both) and confirm PASS (tip vias are exactly what could trip
   V1.b/Vn.b spacing at the 0.41 limit).
3. .va prose (both, on the branches): retire the sparse-via claims -- the "~17% below density[N]"
   SCALE line and the "two errors err in opposite directions / cancel" argument for dropping the
   feed=double residual. Update banner to "N=4 density now EM-corroborated on the via-fixed
   layout" (keep "no silicon"). Fix model_doc's false "re-fit shipped in cap_cmomi.va" claim.
4. Bug (unrelated to vias, surfaced): g2 ac_mom_cap.sch annotation regressed 928.28->872.4 fF
   (commit bdb53ce2, 0.89 pitch misused for finger length); revert text to 928.28 / 1.714 MHz.

## Needs solves -> DEFER (matches the earlier "todavia no")
- The feed=double opposite-side residual (0.40 fF@w2 .. 2.23 fF@w15) is charged at ZERO on the
  "cancels the density deficit" argument. The via fix removes that deficit, so the justification
  collapses. Decide: add it (needs re-measure on fixed cell) or keep zero on a new justification.
- CFEED_SLOPE/END: fitted C(same)-C(none) on sparse cell; tip via is in the core (should cancel
  in the difference) but not re-verified. One confirming solve.

## Electrical goldens / osdi
Unchanged by the via fix (model-derived, coefficients hold). Already regenerated by the row-count
branches for THEIR change. Only re-touch if a coefficient moves (i.e. only if the feed decision
above changes a number).

## Other flags (separate, optional)
- Possible transcription slip: N=4 single-side SRF intercept 8.82e-4 vs neighbours 7.68e-5 /
  2.85e-4 (sets Lcore for feed=same/none N=4). Check vs notes p16.
- No .tcl files exist for cap_cmomi; "the tcl" = the xschem tcleval C= expression only.
